# Week 3 · inventory

**Name:**
**Date of class:** 16 September 2026
**Tool / example:** a ten-item inventory, but the items are yours

This folder already has `inventory.html` and `inventory.css` — a working copy
of the class instrument. **Both files must stay together**; `inventory.html`
loads `inventory.css` from this same folder, not from the class site.

1. See the class version run once as a demo:
   https://kylemath.github.io/Psych302-305-KyleMathewson/inventory.html
2. Open `inventory.html` **in this folder** in VS Code. Find the `ITEMS`
   array in the `<script>` tag.
3. Rewrite every one of the 10 `text` fields — your own statements, not the
   class default. You may rename the five `domain` labels too. Keep the
   shape: 5 domains, 2 items per domain, exactly one `reverse: 1` per pair
   (that is what the scoring code below the comment line assumes).
4. Open the page (double-click, or serve with `python3 -m http.server 8765`
   from the repo root and visit `http://127.0.0.1:8765/week03-inventory/inventory.html`).
   Write your prediction below **before** you click Score.
5. Score, then Export CSV into this same folder.
6. Finish this file.

## Prediction
Which of your domains, and a range you expect, written before you click Score.

## Number
Domain name, mean (after reverse scoring), item count (2). Path to the CSV in this folder.

## What I measured and why
One or two sentences: what did you choose to ask about, and why these ten statements?

## Tedium note
One sentence: what was it like hand-editing ten hardcoded strings inside a
`<script>` tag? What would you rather the items lived in instead (a
separate file, a form builder, something else)?

## Limitation
One reason a self-report number can lie.

## Copilot disclosure
- **Used tonight:** yes / no
- **What I asked** (one sentence):
- **What I kept:**
- **What I changed:**

If Copilot was unavailable, write “no Copilot tonight” and stop. Do not purchase a plan.
